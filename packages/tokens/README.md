# @nepal-gov/tokens

Pulls design-guidelines@tag and generates CSS variables per display mode (light/dark/high-contrast/color-blind-safe/large-text/reduced-motion). The bridge from policy to code — no product code should hardcode visual values.

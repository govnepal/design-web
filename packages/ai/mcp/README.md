# mcp

MCP server exposing the design system to AI agents: get_token (per display mode), get_component, get_pattern, get_icon, search_guidelines (ne/en), validate_snippet, check_page. Wraps the design-tools rules-engine. `local/` = npx @nepal-gov/design-mcp; `remote/` = hosted endpoint. Always reports the guidelines version it serves.

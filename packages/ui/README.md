# @nepal-gov/ui

Civic Calm component library. Mode-aware via CSS variable switching; every component maps to its design-guidelines spec id and ships with keyboard behavior, focus states, and ARIA per its spec. Tested keyboard-only and in every display mode.

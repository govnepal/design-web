# checks-accessibility

Automated accessibility checks: axe-core mapped to WCAG 2.2, keyboard-trap and focus-visibility probes, contrast validation across ALL display modes. Fully deterministic — findings are trusted, not advisory.

# checks-design

Design conformance: colors/spacing/typography actually used vs approved tokens, type scale, tap-target sizes, breakpoints, and emblem misuse detection (recolor/distortion) driven by the identity specs.

# rules-engine

Loads rules/ from design-guidelines@tag and maps rule ids to checker implementations. Published as a package so design-ai and other consumers can validate snippets/pages. Every report states the guidelines version checked against.

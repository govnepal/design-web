# apps/checker

Compliance tool UI: submit a URL, get a scored report per rule — severity, evidence screenshots, and fix guidance (en/ne). Thin frontend over the design-tools API; all evaluation logic lives there so CLI, API, and web UI can never disagree.
